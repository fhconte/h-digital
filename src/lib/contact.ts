import { BRAND_NAME } from "@/lib/brand";

export const CONTACT_EMAIL = "hdgtldevelopment@gmail.com";

export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}?subject=${encodeURIComponent(`Contato - ${BRAND_NAME}`)}`;
